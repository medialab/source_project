
<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" type="text/css" href="assets/css/screen.css">
    <meta charset="utf-8">
  </head>
  <body class="container">
    <h2>source corpus</h2>
    <p>{{date}}</p>
     <hr>
    {{#each corpus}}
      <div class="row">
        <h4 class="col-sm-4"><a href="{{template}}_{{@key}}.html">{{title}}</a> </h4>
        <p class="col-sm-1">{{template}} </p>
        <p class="col-sm-1"><a href="data/{{template}}_{{@key}}.json"  target="_blank">json</a> </p>
        <a class="col-sm-1" href="http://heurist.sydney.edu.au/h4-ao/?db=meder_test_to_delete&q={{query}}" target="_blank">heurist</a>
        <pre class="col-sm-5">{{query}}</pre>
      </div>
      <hr>
    {{/each}}
  </body>
</html>
